/** @format */

// const Todo = require('../models/Todo')

module.exports = {  
  getSettings: async (req, res) => {
    try {
      // will need to store settings in db
      // grab settings from db
      // send them as json

    } catch (err) { console.err(err) }
  },

  changeTheme: async (req, res) => {
    try {

    } catch (err) { console.err(err) }
  },

  changeDefaultPriority: async (req, res) => {
    try {

    } catch (err) { console.err(err) }
  },
}
